upstream api {
    server backend:8000;
}

server {
    listen 80;

    location / {
        root /var/www/react;
    }

    location /admin/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $http_host;
    }
}
