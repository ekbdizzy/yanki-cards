import { withLayout } from '../../layout';
import React, { useEffect } from 'react';
import { API, withBaseUrl } from '../../api';

const MainPage = () => {
  return <h>Main Page</h>;
};

export default withLayout(MainPage);
